# Bu dosyayı kopyalayıp ".env" adıyla kaydedin ve kendi değerlerinizi girin.
# ".env" asla git'e gönderilmez (.gitignore'da).

# --- Her zaman zorunlu ---
NODE_ENV=development
PORT=4000
# mock: Firebase'e hiç bağlanmadan, bellek içi sahte veritabanıyla çalışır.
# live: gerçek Firebase projesine bağlanır (aşağıdaki FIREBASE_* değerleri de zorunlu olur).
FIREBASE_MODE=mock
# mock: WhatsApp/Instagram'a hiç bağlanmadan sahte gönderim yapar (outbox'a yazar).
# live: gerçek Meta Graph API'ye bağlanır (aşağıdaki WHATSAPP_*/INSTAGRAM_* zorunlu olur).
INTEGRATIONS_MODE=mock
# İzin verilen origin'ler, virgülle ayrılmış. "*" yasak (güvenlik).
CORS_ORIGINS=http://localhost:5173,http://localhost:5183

# --- Sadece FIREBASE_MODE=live iken zorunlu ---
# Firebase Console > Proje Ayarları > Hizmet Hesapları > "Yeni özel anahtar oluştur"
# ile indirdiğiniz JSON dosyasındaki alanlar:
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Not: private key'in içindeki satır sonlarını (\n) olduğu gibi tırnak içinde yapıştırın.
FIREBASE_PRIVATE_KEY=
FIREBASE_STORAGE_BUCKET=

# --- Sadece INTEGRATIONS_MODE=live iken zorunlu ---
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
# Meta webhook doğrulama sırasında (hub.verify_token) karşılaştırılan, sizin belirlediğiniz gizli metin.
WHATSAPP_VERIFY_TOKEN=
# Meta App'inizin "App Secret"ı — gelen webhook isteklerinin imzasını (X-Hub-Signature-256) doğrulamak için.
WHATSAPP_APP_SECRET=
INSTAGRAM_APP_SECRET=
INSTAGRAM_VERIFY_TOKEN=
INSTAGRAM_ACCESS_TOKEN=

# --- Opsiyonel (verilmezse makul bir varsayılan kullanılır) ---
LOG_LEVEL=info
SESSION_COOKIE_NAME=session
# "Beni Hatırla" işaretliyse (varsayılan 14 gün, Firebase'in izin verdiği
# tavan), işaretlenmezse (varsayılan 1 gün) kullanılan oturum süresi.
SESSION_COOKIE_REMEMBER_DAYS=14
SESSION_COOKIE_DEFAULT_DAYS=1
SIGNED_URL_EXPIRY_SECONDS=900
META_GRAPH_API_VERSION=v21.0
